// This component is no longer used in the new "Pro Tool" layout as the
// preview functionality has been merged directly into the DiffWorkspace toolbar.
// Keeping it empty or exporting null to satisfy any remaining imports.
export function DeletePreview() { return null; }